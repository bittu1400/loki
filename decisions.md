# Decisions Log

Fast-scan ledger. One line per decision, newest at the bottom.
Full context, alternatives, and consequences live in [adr.md](adr.md).

**Rule:** every decision the author makes is written here the moment it is
made, in the same session, before any other work continues. Nothing waits
for "later". If it is not in this file, it was not decided.

| # | Date | Decision | ADR |
|---|------|----------|-----|
| 1 | 2026-08-24 | v1 = core pipeline only. Publishing endpoint, GitHub Actions cron, and `new_book.py` as an interactive CLI are all deferred. | [ADR-0001](adr.md#adr-0001--v1-scope-boundary) |
| 2 | 2026-08-24 | Python toolchain is `uv` + `pyproject.toml` + Pydantic v2 + ruff + pytest. | [ADR-0002](adr.md#adr-0002--python-toolchain) |
| 3 | 2026-08-24 | One chapter per session, manually triggered. Not two, not scheduled. | [ADR-0003](adr.md#adr-0003--session-shape) |
| 4 | 2026-08-24 | Vault lives at `vault/<book-slug>/` in this repo. Real manuscripts are gitignored; a committed `vault/example-book/` fixture serves tests and CI. | [ADR-0004](adr.md#adr-0004--vault-location-and-what-gets-committed) |
| 5 | 2026-08-25 | Config is split: `models.yaml` for model routing only, `pipeline.yaml` for behaviour (`target_words`, `auto_publish`, etc.). Resolves OQ-03 in favour of the specs.md §9–10 proposal; removes the `PROPOSED` marker there. | — |
| 6 | 2026-08-25 | `vault/example-book/` fixture is an invented throwaway story, deliberately awkward (odd names, tricky continuity), not the author's real book. Resolves OQ-05.1; OQ-05.2 (the real book) stays open until Phase 3 nears. | — |
| 7 | 2026-08-25 | Model routing settled after live spikes: drafting primary `openrouter:minimax-m3:free` with `nvidia:minimaxai/minimax-m3` as same-model mirror; `gemini:gemini-3.5-flash-lite` for brannec-tull POV chapters; editorial `gemini flash-lite` → `mistral:mistral-large`. Full table in example-book `config/models.yaml`. **The editorial half is superseded by #28 and then #31** (2026-09-01): `mistral-large-latest` returns 403 `tier_not_allowed` on the same key, and the fallback is now `mistral-medium-latest`. The drafting half stands. | — |
| 8 | 2026-08-25 | Provider stability principle: every route needs a fallback on a **different provider**, ideally the same model served twice. Provider diversity is a requirement, not an optimisation. Dismissed providers (cohere, z.ai, cerebras, github-models) are commented out of `.env` with reasons and must not be re-added without new evidence. | — |
| 9 | 2026-08-25 | AiHubMix added as sixth provider (`AIHUBMIX_API_KEY`), slotted into the drafting fallback chain between nvidia and groq. Direct MiniMax platform key was evaluated and rejected: pay-as-you-go only, violates the hard 0-cost constraint. Free lanes for minimax-m3 are now openrouter → nvidia → aihubmix. | — |
| 10 | 2026-08-25 | Terminal case — every provider exhausted: the session still writes output, never silently fails and never fakes success. It writes a clearly-marked failed-draft stub chapter (manifest status stays `planned`, stub is excluded from continuity) at zero cost. | [ADR-0005](adr.md#adr-0005--all-providers-failed--stub-draft) |
| 11 | 2026-08-25 | Six new provider keys evaluated: chutes, siliconflow, nanogpt, fireworks, portkey all **dismissed** with dated reasons in `.env` (no needed models on free terms / no free tier / account suspended / redundant gateway). Requesty **kept but key invalid** — regenerate it; its 12 zero-priced models are a candidate last-resort lane pending a prose check of nemotron-3-super-120b (ultra variant already dismissed as canon-breaking). No routing changes until the key works and a spike passes. | — |
| 12 | 2026-08-25 | AiHubMix demoted out of routing: its free tier is ~10 lifetime requests unrecharged (empirically exhausted during the OQ-04 re-run spike), after which all free models return an abuse string. Fails the author's stability condition. Drafting fallback chain returns to nvidia → groq. Spike re-run found no model beating minimax-m3 → primary unchanged. Chutes ($0 balance, paid-only TEE catalog) and SiliconFlow ($0 list rotated away) confirmed dismissed. Full table in open-questions.md OQ-04. | — |
| 13 | 2026-08-26 | Canon reads fail loudly: a FACTS line inside the continuity-tracker markers that does not match the line grammar aborts the session (`ContextError`) instead of being skipped. A fact that cannot be parsed cannot be retrieved, and silently dropping it corrupts canon by omission — the read-side twin of fail-closed writes. Same policy for template slots: an unknown slot, or a builder value with no slot, is a hard error before any API call. | — |
| 14 | 2026-08-26 | Locked-fact retrieval rule (implementing architecture §5): `character:<id>` facts where id = POV or a character named in the beat come first, then unscoped facts whose text mentions one of those characters' name tokens (≥4 chars); file order preserved within groups; capped at `context.max_locked_facts`. Pure-world facts that name nobody are excluded — the story bible covers them, and retrieval-not-dumping is the whole point (pitfall A3). | — |
| 15 | 2026-08-26 | `generated_hash` is owned by `vault.write_chapter` and computed over the exact post-frontmatter bytes as stored — leading blank lines stripped, trailing newline INCLUDED. Callers who supply one are rejected; the primitive re-reads the file after writing and verifies the hash against what landed. Refines the Session 2 convention so post-write verification cannot fail by construction. | — |
| 16 | 2026-08-26 | Manifest status flips are byte-surgical: `flip_manifest_status` splits the row on `|`, replaces only the status cell, and refuses to write unless a post-check proves exactly one line changed at exactly that cell. The engine's sole permitted edit to plot-outline.md stays mechanically provable. | — |
| 17 | 2026-08-26 | Overwrite protection is two-layered: `vault.write_chapter` refuses existing files without an explicit opt-in, and the CLI's `--force` additionally requires typed interactive confirmation (`replace`). With no TTY attached, `--force` refuses closed rather than proceeding — non-interactive force is deferred to the automation phase (ADR-0001) where it can bind to real approval. | — |
| 18 | 2026-08-26 | `--dry-run` output is plain `print()`, not rich markup, and exits before providers are even constructed. Prompt iteration must stay diffable, pasteable, and free. Also reachable via `DRY_RUN=1` per specs §15. | — |
| 19 | 2026-08-26 | An all-routes-exhausted session exits 1 AFTER writing its ADR-0005 stub and audit JSON. Automation sees failure via exit code; the human still finds the marked artifact on disk. Success exits 0 even when the draft is short of target (the cap accepted it) — shortfall is recorded in frontmatter, not punished. | — |
| 20 | 2026-08-26 | Live-run chapters (003, 004) were committed INTO the example-book fixture at the author's direction, so future context-builder runs retrieve a richer vault; provenance frontmatter keeps them distinguishable from hand-written canon. Consequence: tests must not depend on fixture growth — `tests/fakes.py::reset_fixture_state()` forces any copied book back to the canonical drafting-test state. | — |
| 21 | 2026-08-29 | TokenRouter (`api.tokenrouter.com`, a one-api relay) **dismissed**, key commented out of `.env` per decision #8. The key is dead — one-api gates on the token's own RemainQuota before model pricing, so all three free models return quota-exhausted; verified with live POSTs. Fixing the quota would still buy only one plausible free drafting lane (`z-ai/glm-5.3-free`) out of a 133-model catalog with 3 zero-priced entries, behind a relay whose quota already died once — the aihubmix pattern from #12. No routing changes. | — |
| 22 | 2026-08-31 | Style-check thresholds live in a `<!-- THRESHOLDS -->` delimited block inside each book's `style-guide.md`, parsed with the same marker discipline as MANIFEST/FACTS. A book with no block gets **metrics reported and verdicts skipped** — there are no built-in numeric defaults anywhere in code. Resolves OQ-09. Hidden defaults would smuggle creative constants into the engine (specs §14) and make an untuned book look tuned; an absent block is a visible, explicit state. The example-book fixture gains a block so Batches 2–3 have something to test against. | — |
| 23 | 2026-08-31 | Prompt template gains a **Rhythm (hard requirement)** block: explicit sentence-length spread (mean near 14, some under 6, at least three past 30) and an opening-variety rule (no more than two consecutive sentences opening on the same word; under one in five opening with He/She). Measured on a local gemma-4-12b spike against the identical ch-003 prompt: mean 9.7 → 12.4, TTR 0.354 → 0.392, He-openings 58 → 35, word count 1389 → 1140, and it became the only draft of three to pass every declared threshold. The staccato was the prompt, not the model. To be verified live on minimax-m3 (whose defect is the opposite — 19.6 mean) before the change is trusted for the primary drafting lane. | — |
| 24 | 2026-08-31 | Local llama.cpp lane adopted as the **last-resort drafting fallback**, below groq. `providers/local.py` over the existing OpenAI-compatible client, `http://localhost:8080/v1`, no API key. Rationale is availability, not prose: a local model has no quota, no rate limit, and cannot be renamed or withdrawn — it is immune to every failure mode that killed aihubmix (#12) and tokenrouter (#21). Cost: the lane is dead whenever the server is not running, which is an offline dependency the remote lanes do not have. The route's token budget must fit the server's n_ctx (8192 today) rather than the book-wide 12000. | [ADR-0006](adr.md#adr-0006--local-model-lane) |
| 25 | 2026-08-31 | The ch-005 self-contradiction ("Both of them" then "nine corrections", against ch-001's canonical two) was fixed by **hand-editing the prose and leaving `generated_hash` untouched**. specs §3 makes the hash immutable exactly so a stale hash reports "the author edited this" — rewriting it would have destroyed the Phase 6 author-edit signal (pitfalls B5) to make a test pass. `tests/test_vault_writing.py` now asserts BOTH halves of the contract: unedited chapters match, and `AUTHOR_EDITED_CHAPTERS` (ch-005) must NOT match. The pre-fix text survives in git at `d518b74` and stays the Phase 5 editorial test case. | — |
| 26 | 2026-09-01 | The **editorial prompt template is engine-owned and packaged** (`novel_engine/templates/editorial-prompt.md`), not copied into each book's `config/` the way the drafting template is. The editorial prompt is a JSON contract plus assembled evidence — chapter text, retrieved facts, the beat, the character sheet, the Phase 4 metrics — with nothing creative in it to tune, and a book-local edit could break the schema contract silently, spending `max_repair_attempts` and free-tier quota on every session before failing closed. Cost accepted: a book cannot change how its editor is asked. Deliberately NOT the "packaged default + optional book override" shape — two code paths and two templates to keep in sync for a file nobody has asked to tune. | — |
| 27 | 2026-09-01 | Thread ID allocation is **one above the highest ID currently in `open-threads.md`**. Under engine operation IDs never repeat: the engine only appends and only flips status, so a resolved thread keeps its line and its number. An author who hand-deletes a thread line frees that number back, which the engine cannot see — recorded as an author action outside the engine's guarantee (specs §5) rather than closed with a persisted counter, which would add a third mechanical edit path and its own failure modes to plug a hole only a hand-deletion opens. | — |
| 28 | 2026-09-01 | **Editorial routing reordered: `mistral:mistral-medium-latest` primary, `gemini:gemini-3.5-flash-lite` fallback.** Decided on the first real continuity case, not on marketing: pointed at the original ch-005 ("nine corrections" against ch-001's locked "two", with that fact in the prompt), flash-lite returned an empty violation list twice — once writing the contradiction into the summary it appended to canon — while mistral-medium quoted the contradicting sentence and named the violated fact on the first call with zero repairs. Costs ~2.7x the output tokens (1091 vs 400). `mistral-large-latest` is **dead**: 403 `tier_not_allowed` (code 1910) on the same key that verified it in Session 4, though `/v1/models` still lists it — the catalog is not the entitlement. Editorial is the one role where judgement beats latency, so the model that judges leads and the cheap one catches the outage. **Superseded the same day by #31** — the premise ('flash-lite cannot catch this') stopped being true once #30 shipped. | — |
| 29 | 2026-09-01 | **A delta carrying a `critical` continuity violation is not reconcilable.** `reconcile()` refuses it whole: nothing is appended, the chapter stays `editorial-pending`, and the author fixes the prose or demotes the fact first. Found live — mistral-medium flagged "nine vs two" as critical and, in the same delta, proposed `The page carries nine corrections` as a NEW LOCKED FACT. Without this rule the pass that detects a contradiction is also the fastest route for that contradiction to become canon. Rejected alternatives: dropping only the contradicting facts (matching model text against model text, and half-applying a delta the model itself says describes a broken chapter) and appending everything (retrieval would then feed both sides of the contradiction into the next chapter's prompt). | [ADR-0009](adr.md#adr-0009--a-chapter-that-contradicts-locked-canon-is-not-reconcilable) |
| 30 | 2026-09-01 | **A deterministic number check runs before the editorial call.** Python extracts quantities from the retrieved locked facts and flags chapter sentences that state a different number for the same noun; the findings go into the editorial prompt as evidence, the same way Phase 4's metrics do. Built even though #28's primary catches the observed case: it costs no quota, runs whichever editor answers (the fallback demonstrably misses), does not depend on a free tier staying alive, and attacks exactly the class — a bare number disagreement — that was missed twice. It is evidence for the model, never a gate on the chapter (specs §14's rule extended). | [ADR-0008](adr.md#adr-0008--continuity-checking-is-not-exclusively-the-models-job) |
| 31 | 2026-09-01 | **Editorial routing reverted: `gemini:gemini-3.5-flash-lite` primary, `mistral:mistral-medium-latest` fallback.** Supersedes #28 on the same day and on better evidence. With the deterministic number check (#30) in the prompt, flash-lite caught the ch-005 contradiction on **both** live runs — the case it had missed twice unaided — quoting the sentence and naming the fact. It costs 476 output tokens against mistral-medium's 1091, and proposed 1 new locked fact against mistral's 8-9 (including set dressing like "the Almanac Office uses green tape"), which matters because every accepted fact is permanent ledger growth (pitfall A3). Mistral-medium stays as fallback precisely because it is the only model that has caught a contradiction UNAIDED — the right property for the lane that runs when the primary is down. Known residual: flash-lite's catch now depends on the pre-filter, so a contradiction the regex cannot see (a name, a date, a rewritten quantity) is still unproven — OQ-10's remaining scope. | — |
| 32 | 2026-09-01 | **The editorial prompt constrains what a new locked fact IS**: something a later chapter could contradict — a count, a date, a name, a capability, a physical state — never atmosphere and never a detail mentioned once in passing. Prompt rule only; no `max_facts_per_delta` config knob and no schema cap. Rejected the cap because it puts an arbitrary number in every book's config and spends a repair round when it fires, to enforce a judgement the prompt can state directly. Accepted risk: this is a rule a model can ignore, exactly as it ignored the violation instruction twice — so ledger growth stays something to watch, not something now solved. | — |
| 33 | 2026-09-03 | **`write_next_step` is the only overwrite primitive in `core/vault.py`.** `log/next-step.md` is the sole canon-adjacent file whose mode is overwrite (architecture §3). In accordance with the one-writer rule (invariant 1), writes must go through `vault.py`. `write_next_step()` verifies round-trip by re-reading and re-parsing what landed on disk. `test_vault_exposes_no_general_canon_writer` is updated on purpose to assert it. | [ADR-0010](adr.md#adr-0010--session-pointer-persistence-and-resumption-state-machine) |
| 34 | 2026-09-03 | **Every session phase transition is validated against the specs §11 lifecycle and persisted to disk *before* the subsequent phase begins.** Enforced via `validate_transition()` and `SessionStateMachine.transition()`. If the session pointer is marked `blocked: true`, any phase transition is refused with `StateMachineError` until explicitly unblocked. | [ADR-0010](adr.md#adr-0010--session-pointer-persistence-and-resumption-state-machine) |
| 35 | 2026-09-04 | **`flip_chapter_status` is the second single-cell mechanical edit in `core/vault.py`.** A completed session flips one chapter frontmatter key, `status: draft` -> `pending-review` (specs §3, architecture §4 step 11), guarded by `expected_current` and re-parsed after the write like every other primitive. The chapter BODY is not touched, so `generated_hash` — which hashes post-frontmatter bytes only — stays valid and the author-edit signal (#25) survives a status flip. `test_vault_appends.py`'s writer allowlist is updated on purpose. Rejected leaving chapters at `draft` forever: it would have left the pipeline unable to express "this chapter is finished and awaiting review", which is the entire meaning of the `complete` phase. | [ADR-0011](adr.md#adr-0011--write-session-orchestration-resume-gate-and-outcome-contract) |
| 36 | 2026-09-04 | **`editorial.enabled: false` is honoured, and `styled -> complete` becomes a legal transition used only on that path.** The key had been parsed-but-unread since Phase 5 (specs §10) and Phase 6 is where it was always going to be consulted. Without the new edge the specs §11 diagram has no exit from `styled` except through the editorial pass, so a book that switches the pass off would park its pointer at `styled` and resume the same chapter forever. Rejected routing disabled-editorial to `editorial-pending`: nothing is pending, and a pointer that says otherwise lies to the next session. The escape also gives a drafting-only mode that writes no canon, which is the shape a real vault needs while OQ-01 is open. **OQ-01 closed the same session (#40); this mode is now what a book runs when the author wants drafts without a continuity ledger, and the one configuration allowed to proceed with no git (#41).** | [ADR-0011](adr.md#adr-0011--write-session-orchestration-resume-gate-and-outcome-contract) |
| 37 | 2026-09-04 | **`write-session` exits 2 when a chapter is drafted but the editorial pass did not reconcile.** specs §15 had only 0 and 1; conflating `editorial-pending` with either is a lie in both directions — the chapter exists on disk (not a refusal, so not 1) and canon was deliberately left untouched (not success, so not 0). Exit 2 means exactly one thing: prose written, canon untouched, session resumable with `--resume`. This is the machine-readable half of Phase 6's "visibly distinct from success" requirement; the human half is the CLI naming which of the three ADR-0009/§11 routes to `editorial-pending` was taken. | [ADR-0011](adr.md#adr-0011--write-session-orchestration-resume-gate-and-outcome-contract) |
| 38 | 2026-09-04 | **Resuming an interrupted session requires `--resume`; a bare re-run refuses.** The refusal names the chapter, its recorded phase, and the exact flag to type. specs §11 says the engine "resumes from the recorded phase, or refuses with a precise message" — the flag is what selects between the two. Rejected automatic resumption: a bare `write-session --book x` that silently spends free-tier calls continuing a chapter the author had forgotten about is the same class of surprise as an unconfirmed overwrite, and free-tier daily caps are the hardest constraint in the project. `--resume` never drafts over existing prose; replacing prose still requires `--force` and its typed confirmation (invariant 5). | [ADR-0011](adr.md#adr-0011--write-session-orchestration-resume-gate-and-outcome-contract) |
| 39 | 2026-09-04 | **A deterministic entity check runs before the editorial call, beside the number check.** `quality/continuity_entities.py` flags a chapter paragraph that carries a locked fact's distinctive wording while naming a DIFFERENT known character, and hands the finding to the prompt as evidence — the ADR-0008 pattern, applied to the class OQ-10's name experiment just measured. Built on evidence, not symmetry: `gemini-3.5-flash-lite` missed a planted identity contradiction twice at temperature 0.2 with the violated fact in the same prompt, and caught it as `critical` on the first call once one simulated entity finding was added. Instructions were identical across all three runs, so the lever is the evidence, not the wording. Unlike the number check this one scans PARAGRAPHS: the planted case put the fact's noun phrase in one sentence and the wrong name in the next, and sentence-scoped matching missed its own test case. Evidence for the model, never a gate (specs §14). | [ADR-0012](adr.md#adr-0012--the-deterministic-layer-extends-to-entity-disagreements) |
| 40 | 2026-09-04 | **OQ-01 resolved: every real book is its own git repository at `vault/<slug>/.git`, and the engine commits twice per session** — the author's edits before it starts, its own writes after it ends. Adopts the recommendation open since Session 5 unchanged. Two commits, not one, because a single commit mixes the author's morning into the engine's session and makes "undo that session" undo their work too. **Restore is deliberately NOT implemented**: an engine that can check out old content is an engine that can overwrite author prose without `--force` (invariant 5), and `git -C vault/<slug> checkout HEAD~1` already does it better than a wrapper would. **No remote is ever configured and nothing is ever pushed** — ADR-0004's privacy property is not for sale. A book already tracked by an enclosing repo (`vault/example-book/`) is skipped rather than double-versioned. | [ADR-0013](adr.md#adr-0013--every-real-book-is-its-own-git-repository) |
| 41 | 2026-09-04 | **A session that cannot snapshot and would write canon is refused; one that writes no canon warns and continues.** No git on PATH means no recovery path, which is the exact condition OQ-01 exists to prevent — so `write-session` exits 1 before the first write rather than proceeding while looking safe (pitfall A6's shape: a safety net that reports success while catching nothing). The exception is `editorial.enabled: false`, which appends nothing to canon and whose chapter write is create-only, so the loss is bounded and the session proceeds with a warning. Rejected refusing in both cases (it makes a missing tool block drafting that was never at risk) and warning in both (it lets canon be written with no undo, which is the thing being fixed). | [ADR-0013](adr.md#adr-0013--every-real-book-is-its-own-git-repository) |

## Pending

Decisions that are known to be needed but not yet made are tracked in
[open-questions.md](open-questions.md), not here. This file records only
what has actually been settled.
